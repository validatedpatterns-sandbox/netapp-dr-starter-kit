# -----------------------------------------------------------------------------
# S3 Backend Configuration for VPC Peering Terraform State
# Generated by Ansible - Do not edit manually
# -----------------------------------------------------------------------------

terraform {
  backend "s3" {
    bucket         = "{{ vpc_peering_terraform_state_bucket }}"
    key            = "{{ vpc_peering_terraform_state_key }}"
    region         = "{{ prod_cluster.region }}"
    encrypt        = true
    dynamodb_table = "{{ vpc_peering_terraform_state_dynamodb_table }}"
  }
}
