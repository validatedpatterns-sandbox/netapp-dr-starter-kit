{{- if .Values.crossplane.providerConfig.credentials.fromVault | default false }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: aws-provider-creds
  namespace: {{ .Values.crossplane.providerConfig.credentials.secretNamespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ .Values.crossplane.providerConfig.credentials.secretName }}
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        {{ .Values.crossplane.providerConfig.credentials.secretKey }}: |
          [default]
          aws_access_key_id = {{`{{ .accessKeyID }}`}}
          aws_secret_access_key = {{`{{ .secretAccessKey }}`}}
  data:
    - secretKey: accessKeyID
      remoteRef:
        key: secret/data/global/aws
        property: accessKeyID
    - secretKey: secretAccessKey
      remoteRef:
        key: secret/data/global/aws
        property: secretAccessKey
{{- end }}
